export interface Schema {
    // The name of the feature.
    name: string;

    // The path to create the feature.
    path?: string;

    // The name of the project.
    project?: string;
}
